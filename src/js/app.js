// Custom Alpine.js data and stores
// This file loads before Alpine, so you can define Alpine.data() components here

document.addEventListener('alpine:init', () => {
  // Example: Register a reusable component
  // Alpine.data('dropdown', () => ({
  //   open: false,
  //   toggle() { this.open = !this.open }
  // }))
})
